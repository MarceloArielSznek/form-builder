# Example Nginx reverse proxy for the form-builder SPA (PM2 / Vite preview on 127.0.0.1:4173).
# Copy to sites-available and replace server_name with your host (e.g. forms.example.com).

server {
    listen 80;
    listen [::]:80;
    server_name forms.example.com;

    location / {
        proxy_pass http://127.0.0.1:4173;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    }
}

# TLS on the origin (e.g. Cloudflare Full strict): set real paths to cert/key.
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name forms.example.com;
#     ssl_certificate     /path/to/fullchain.pem;
#     ssl_certificate_key /path/to/privkey.pem;
#     location / {
#         proxy_pass http://127.0.0.1:4173;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         add_header X-Content-Type-Options "nosniff" always;
#         add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#     }
# }
